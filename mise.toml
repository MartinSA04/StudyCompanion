# Toolchain for the study-companion dev container. `mise install` (run in the
# devcontainer postCreate step) provisions these. pnpm is pinned to match the
# `packageManager` field in package.json; Node 22 is the current LTS and
# satisfies Astro 6.
[tools]
node = "22"
pnpm = "10.33.0"
