/**
 * DEV / DEMO HARNESS content config (this `demo/` folder is the harness's
 * Astro `srcDir`, set in astro.config.mjs — kept separate from the library's
 * `src/` so the framework's own `src/pages/` aren't auto-routed alongside the
 * injected routes).
 *
 * A real course repo has the identical one-liner at its own `src/content.config.ts`.
 */
export { collections } from "study-companion/content";
